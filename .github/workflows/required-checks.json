{
  "_comment": "duplication-gate removed from required (2026-07-02): flaky at token boundaries (false 8-line self-clones not reproducible locally), blocked BET-57/PR#27, and agents cannot rerun CI. It still runs in CI as advisory; the reviewer independently checks for duplication.",
  "required_checks": [
    "typecheck-test",
    "secret-scan",
    "dep-audit"
  ]
}
